<?php
$id = get_field('id');
?>

<section
    class="timeline"
    id="<?php if (empty($id) === false) {
        echo $id;
    } ?>"
>
    <div class="container">
        <h3>Redesign for timeline component is pending. Dev on hold.</h3>
    </div>
</section>
